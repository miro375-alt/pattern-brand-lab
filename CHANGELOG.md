# CHANGELOG — 업무툴

> 프롬프트빌더 CHANGELOG 문화 이식: 단순 변경 목록이 아니라 **"왜 이렇게 결정했는가"의 자산**.
> 형식: newest-first · 날짜 절대표기 · 제목은 그 버전의 정체성 한 줄.
> 사업 차원의 방향 전환은 여기가 아니라 [../기획/결정기록.md](../기획/결정기록.md) (여긴 툴 이력만).

---

## v5.0 — 백지 재설계: 8탭을 4화면으로 (2026.08.11)

**정체성**: "기능 목록"에서 "하루의 동선"으로.

- 로드맵/스케줄러/캘린더는 별개 기능이 아니라 **한 데이터의 세 얼굴**이라는 결론(기획/업무툴_설계.md) → 4화면(오늘/일/자료/기록)으로 통합. 데이터도 7배열→3배열(tasks/items/log).
- **활동 피드 신설** — 업무공유의 빠진 조각: 동기화는 되는데 "상대가 뭘 했는지"를 알 수 없던 문제. 체크·저장이 자동으로 로그를 남겨 「오늘」 맨 위에 뜸(추가 입력 0).
- **소스 분할 + 빌드 조립**: srcdoc은 base URL이 about:srcdoc이라 ES 모듈 불가 → src/를 쪼개고 build.mjs가 연결(전역 스코프 공유, SCRIPTS 순서=의존성). 비밀은 `.secrets.json`에서 주입.
- 디자인 = 사용자의 프롬프트빌더 언어 이식(검정 주색·pill·점선·큰 번호 카드), 액센트만 인디고. → DESIGN.md
- 잡은 함정: TPL 시드 all-or-nothing(부분 데이터면 42건 미생성)→id 단위 보충 / 「오늘」 빈 화면 문제→다음 작업 5건 당김 / span 이어붙임 레이아웃 실수 반복.

## v4.2 — 레퍼런스·md 문서고·노션 임베드 (2026.08.10~11)

**정체성**: 자료가 툴 안으로.

- 레퍼런스 탭: 노션 등 외부 링크 등록(2인 동기화). **노션 일반 공유링크는 iframe 차단(SAMEORIGIN) — 공식 `/ebd/` 경로(2025.02 출시)로 자동 변환**하면 임베드됨(실측 검증).
- md 렌더러 내장 → 결정기록·금지목록·바이블 인덱스를 문서고에서 직접 열람. 계산기는 메인에서 강등(사용자 지시).
- 실버그: mergeState가 refs 키를 떨어뜨려 "동기화 오류" — 병합 함수는 **모든 상태 키를 명시적으로** 다뤄야 한다는 교훈. SW도 암호문은 네트워크 우선으로 교체(구버전 서빙 문제).

## v4.1 — 디자인 시스템 + 실브라우저 QA (2026.08.10)

**정체성**: 도구에서 제품으로.

- Pretendard·웜 그라운드·사이드바 셸. 헤드리스 스크린샷의 모바일 잘림이 실기기에선 재현 안 됨(헤드리스 최소폭 클램프 아티팩트) → **모바일 검증은 실브라우저로**가 규칙이 됨.
- 페이지 타이틀 미갱신 버그: 문자열 치환 패치가 조용히 실패(assert 없는 replace) → 이후 패치는 assert 필수.

## v4.0 — 디데이 자동 스케줄러 (2026.08.10)

**정체성**: 목표 하나로 일정 전체가 굴러가게.

- 런칭 로드맵 8단계·투두 42건 프리로드, 디데이 역산 배치(off 오프셋), 수동 마감은 📌고정, 지연은 오늘로 자동 재배치, 위험 신호등(주당 필요 페이스).
- 캘린더 오늘/주간/월간 + PWA + 모바일 하단탭.

## v3 — 2인 협업 기반 (2026.08.10)

**정체성**: 혼자 쓰는 메모장에서 둘이 쓰는 공유 공간으로.

- E2E 암호화 동기화(GitHub contents API, sha 조건부 PUT, id별 LWW+툼스톤 병합). 작성자 배지. 할일 칸반.
- NAS 백엔드를 만들었다가 사용자 지시로 즉시 폐기 → **⛔NAS 금지**가 확정된 시점.

## v1~v2 — 암호화 문서고 (2026.08.10)

- 로그인(PBKDF2+AES-GCM) 뒤 문서 열람. "저장소는 공개, 내용은 암호문" 구조 확립. 4자리 pw 취약 경고 고지됨(D-007 부채 B-3).
