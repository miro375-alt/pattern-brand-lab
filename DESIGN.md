# DESIGN — 업무툴 디자인 규칙

> prompt-builder의 DESIGN.md 문화 이식. 원본은 Nike 시스템을 기준 삼았지만, **우리 툴의 기준 시스템 = 사용자의 프롬프트빌더(v13.1_2) 디자인 언어**다(v5에서 이식 완료).
> 원칙 한 줄: **chrome 절제 + 내용(숫자·일정·문서)이 말하게** — 장식 그라디언트·과한 그림자·이모지 남발 금지.

## 토큰

### 컬러
| 역할 | 값 | 규칙 |
|---|---|---|
| 주색(잉크) | 검정 계열 `#111` 근방 | 제목·주요 버튼·활성 상태. "브랜드의 유일한 색"처럼 쓴다 |
| 그라운드 | 웜 오프화이트 | 페이지 배경. 순백 카드가 그 위에 얹힘 |
| 액센트 | **인디고 `#2F4BC4`** | 상호작용(링크·활성 탭·포커스)에만. 톤내기용 사용 금지 |
| 시맨틱 | good/warn/crit 3색 | 신호등·지연·완료 배지 전용. 액센트로 재사용 금지 |
| ⛔ 금지 | 아가드 머스타드/베이지 | 이 브랜드는 아가드와 무관 — 섞지 않는다 |

### 기하·질감
- **pill 기하**: 버튼·칩·배지는 완전 라운드(999px). 카드 모서리는 10~14px 두 단계만.
- **그림자 최소**: 카드 1단계 미세 그림자까지만. 다크모드는 그림자 대신 보더.
- **점선(스티치)**: 구분선은 dashed — 원단 브랜드의 시그니처 디테일. 남발 금지(섹션 경계에만).
- **큰 번호 카드**: D-day·카운트 등 핵심 숫자는 850~900 웨이트 대형으로 — 숫자가 히어로다.
- **uppercase 부제**: 영문 라벨은 letter-spacing 넓힌 소형 대문자.

### 타이포
- **Pretendard** 단일 패밀리(CDN+시스템 폴백). 표시 숫자만 웨이트로 차별화.
- 정렬되는 숫자(날짜·수치 컬럼)는 `font-variant-numeric: tabular-nums` 필수.

## 레이아웃 규칙

- 데스크탑: 좌측 내비 레일 + 페이지 헤더(제목·날짜). 모바일: 하단 탭바 + 상단 미니바.
- 모바일 우선 검증 — **헤드리스 스크린샷은 최소폭 클램프로 거짓 잘림을 보여줌**(v4.1 실사례). 모바일 확인은 실브라우저 리사이즈로.
- 터치 타깃 44px 근방, 입력은 원터치 동선(현장=동대문·봉제처에서 폰으로 쓴다).

## 상태 표현 (UI는 읽는 게 아니라 스캔하는 것)

- 상태는 숫자+형태로 이중 인코딩: 지연=적색 pill, 오늘=인디고, 마일스톤=오렌지 계열 점.
- 신호등(위험도)은 색+문장 병기 — 색만으로 의미 전달 금지.
- 로그인 화면은 브랜드의 문: 워드마크 + 태그라인("아이와 함께 자라는 무늬를 짓는 일") 유지.

## 다크모드

- 반전이 아니라 별도 조색(웜 다크 그라운드). 토큰 단위로만 재정의 — 컴포넌트에 하드코딩 금지.

## 변경 절차

디자인 토큰 변경은 `src/style.css`의 토큰 블록에서만 → 빌드 → 스크린샷(데스크탑+실브라우저 모바일) 확인 → CHANGELOG에 한 줄. 화면별 예외 스타일을 만들기 전에 토큰으로 풀 수 있는지 먼저.
